#pragma once

/*
#include <sphere.h>



class Ball
{
public:
	Sphere sphere_collider;

	float mass;
	vec3 velocity;
	float energy;

	Ball();
	Ball(vec3 position, float radius, float mass, vec3 velocity);


	vec3 computeNextPosition(float deltaTime);
	void updatePosition(vec3 position);
};
*/